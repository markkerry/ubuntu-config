# K8s Lab Manual Steps

* Set static IPs
* Set hostnames
* Edit cluster host names to the hosts file of each VM
* Disable Swap `sudo swapoff -a`
* Comment out /swap.img via `sudo vim /etc/fstab`
* Restart servers
